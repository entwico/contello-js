---
'@contello/store': minor
'@contello/astro': minor
---

`defineCollection` / `defineCollectionSync` / `defineLazyCollection` / `defineSingleton` / `defineSingletonSync` now accept either a string key (from `schema.sources`) or a SourceDef directly
