---
'@contello/store': minor
---

routes and assets ship as three variants each: `defineRoutes` / `defineAssets` (eager async), `defineRoutesSync` / `defineAssetsSync` (eager + sync), `defineLazyRoutes` / `defineLazyAssets` (lazy)
