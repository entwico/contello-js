---
'@contello/store': major
---

Returned values from `Collection`, `CollectionSync`, `Singleton`, `SingletonSync`, `LazyCollection`, `Assets`, `Routes`, and `I18nMessages` are typed `ReadonlyDeep<V>` (re-exported from `projected`)
