---
'@contello/astro': minor
'@contello/client': minor
'@contello/store': minor
---

collections, routes and assets carry the writes their schema defines — `create` / `update` / `delete`, typed from the model's own inputs
