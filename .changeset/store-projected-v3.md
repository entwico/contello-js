---
'@contello/store': major
---

`projected` moved from a peer dep to a regular dep, and bumped to `^3.0.0`. The relevant utilities — `MaybePromise`, `maybeThen`, `maybeCatch`, `ReadonlyDeep` — are re-exported from `@contello/store`.
